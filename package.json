{
    "name": "tinygpt",
    "version": "0.0.1",
    "description": "A tiny completion model",
    "main": "dist/tinygpt.js",
    "author": "antoineMoPa",
    "license": "MIT",
    "scripts": {
        "build": "tsc src/*.ts --outDir dist",
        "testbench": "rm -rf dist/* && yarn run lint && yarn run build && node dist/testbench.js",
        "testbench:debug": "rm -rf dist/* && yarn run lint && yarn run build && node debug dist/testbench.js",
        "test": "rm -rf dist/* && yarn run lint && yarn run build && node dist/tinyindex.js",
        "lint": "eslint . --ext .ts",
        "purgeTrainingData": "rm -rf data/trainingData.json",
        "purgeModel": "rm -rf data/wordPredictModel",
        "purge": "yarn run purgeTrainingData && yarn run purgeModel",
        "buildCorpus": "node dist/buildCorpus.js",
        "tfjs:fix": "npm rebuild @tensorflow/tfjs-node --build-addon-from-source"
    },
    "dependencies": {
        "@tensorflow-models/universal-sentence-encoder": "^1.3.3",
        "@tensorflow/tfjs-node": "^4.2.0",
        "@types/lodash": "^4.14.191",
        "@typescript-eslint/eslint-plugin": "^5.50.0",
        "@typescript-eslint/parser": "^5.50.0",
        "big-json": "^3.2.0",
        "chokidar": "^3.5.3",
        "eslint": "^8.33.0",
        "lodash": "^4.17.21",
        "source-map-support": "^0.5.21",
        "typescript": "^4.9.5",
        "wordpos": "^2.1.0"
    }
}
